
export enum StepType {
  CHECKLIST = "CHECKLIST",
  PLAN = "CHECKLIST",
}

export enum LogType {
  INFO = "INFO",
  WARNING = "WARNING",
  ERROR = "ERROR",
}
export enum RequestType {
  CREATE = "CREATE",
  UPDATE = "UPDATE",
  DELETE = "DELETE",
  READ = "READ",
}